namespace Boto.Terminals;

/// <summary>
/// Terminal options.
/// </summary>
/// <param name="Viewport">Teh <see cref="Terminals.Viewport"/>.</param>
public record TerminalOptions(Viewport Viewport);
